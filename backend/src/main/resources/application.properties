server.port=8080

# conexion a Oracle (el usuario se crea con database/scripts/00_crear_usuario.sql)
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
spring.datasource.username=metro_ny
spring.datasource.password=metro123
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.hikari.maximum-pool-size=5

# para que los errores de validacion lleguen con mensaje
server.error.include-message=always

# origenes que pueden llamar a la API (el frontend en desarrollo)
app.cors.origenes=http://localhost:3000,http://localhost:5173

# todos los dias a la 1:00 am se revisan certificaciones y tarjetas vencidas
app.tareas.vencimientos=0 0 1 * * *
